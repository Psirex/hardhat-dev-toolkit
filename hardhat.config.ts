import { HardhatUserConfig } from "hardhat/types";

// we need it in the report to test hardhat functionality
const config: HardhatUserConfig = {
    defaultNetwork: "hardhat"
};

export default config;
